<% form_name ||= "Manage" %>
<h1 class="text-center">  <%= form_name %> lab </h1>
<%= form_for @lab , method: (form_name.downcase == 'edit' ? 'put' : 'post') do |f| %>
  <% if @lab.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@lab.errors.count, "error") %> prohibited
          this article from being saved:
        </h2>
        <ul>
          <% @lab.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  <div class="form-group">
    <%= f.label :lab_name%>
    <%= f.text_field :lab_name, class: "form-control" %>
  </div>
  <%= f.submit "#{form_name} lab", class: "btn btn-primary" %>
  <a href="<%= labs_path %>" class="btn btn-default"> View all </a>
<% end %>
